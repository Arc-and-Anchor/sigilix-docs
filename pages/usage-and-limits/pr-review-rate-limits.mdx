---
title: "PR Review Rate Limits"
description: "The organization pool in detail — how the rolling 5-hour and weekly windows apply to PR reviews, what counts as a review, event coalescing, monitoring, and what happens at the cap."
---

import { Callout, Cards } from 'nextra/components'

# PR Review Rate Limits

Automated PR reviews draw from your **organization pool** — see [Usage & Limits](/usage-and-limits) for the full picture of pools, windows, and per-plan allowances. This page covers the review-specific mechanics: what counts as a review, how bursts are coalesced, and how to monitor usage.

<Callout type="info">
  Sigilix is in **private beta**. Exact per-plan review allowances surface as a percentage in the portal rather than as raw counts; the mechanics below (rolling window, coalescing, monitoring) are accurate. If you need more throughput, [contact us](mailto:support@sigilix.ai).
</Callout>

## The windows

PR reviews are counted against the same two windows every meter uses (see [Usage & Limits → The two windows](/usage-and-limits#the-two-windows)):

- A **rolling 5-hour window** — Sigilix counts how many reviews you've completed in the trailing five hours. It's rolling, not fixed: as older reviews age out, fresh capacity frees up gradually.
- A **weekly window** — total review usage for the week, resetting Monday at 9:00 AM in your local time.

<Callout type="info">
  PR reviews are metered once for the whole organization — a busy teammate's reviews and your own draw from the same org pool. Per-seat work (the CLI agent and chatbot) is a separate pool and never eats into review budget.
</Callout>

## What counts as a "review"

A review is one full pass of the specialist ensemble on a PR. That includes:
- Initial review on `pull_request.opened`
- Re-reviews on `pull_request.synchronize` (each push counts)
- Reviews on `pull_request.reopened` and `pull_request.ready_for_review`

**Mentions don't count.** Replying to `@sigilix` on a comment thread is a lighter pipeline that's not subject to the rate limit.

## Burst behavior

If your team pushes 20 commits to a single PR in 30 minutes, Sigilix sees 20 `pull_request.synchronize` events and tries to run 20 reviews. Without throttling, that would burn your entire budget on one PR.

**Sigilix coalesces synchronize events.** When a synchronize event arrives, Sigilix waits ~30 seconds for additional pushes before starting the review. If more pushes arrive within that window, they're collapsed into one review on the latest SHA. The earlier SHAs aren't reviewed.

This means: a rapid push-fix-push-fix sequence costs one review, not three.

## Hitting the cap

When you hit your rate limit:

1. Sigilix doesn't review the PR
2. A small comment is posted: *"Rate limit reached. Next review available in ~42 minutes."*
3. The review will run automatically once a slot frees up

Old reviews on prior SHAs of the same PR remain visible — they don't disappear when you hit the cap.

## Monitoring usage

### Headers

Every Sigilix-posted review has a footer in its body that reports your current usage against the window:

```
_Sigilix · 2 of 3 reviews used in past 5h · resets in 38m_
```

Use this to gauge how close you are to the cap. Your live usage across all pools and windows is also shown as a percentage in the portal.

### Webhook

For programmatic monitoring, a `review.completed` webhook is available. It includes:

```json
{
  "event": "review.completed",
  "pr": { "owner": "...", "repo": "...", "number": 42 },
  "rate_limit": {
    "window_hours": 5,
    "max_reviews": 3,
    "used": 2,
    "resets_at": "2026-05-05T03:42:00Z"
  }
}
```

See [Webhooks](/api-reference/webhooks) for setup.

## Hitting the limit regularly

If reviews queue during peak hours, or you're hitting the cap multiple days per week, your account allowance may need raising. During the private beta, throughput is provisioned per account — [contact us](mailto:support@sigilix.ai) and we'll adjust it.

## Read next

<Cards>
<Cards.Card title="Usage & Limits" href="/usage-and-limits">
    The two pools, the rolling 5-hour and weekly windows, and per-plan allowances.
  </Cards.Card>
<Cards.Card title="sigilix.json" href="/configuration/configuration-reference">
    Full configuration schema.
  </Cards.Card>
<Cards.Card title="Join the private beta" href="https://sigilix.ai">
    Pricing (per-seat + usage, BYO models) is handled per account during the beta.
  </Cards.Card>
</Cards>
